<%= form_for @event do |e| %>
	<%= e.label :name %>
	<%= e.text_field :name %>

	<%= e.label :location %>
	<%= e.text_field :location %>

	<%= e.label :date %>
	<%= e.text_field :date %>

	<%= e.label :schedule %>
	<%= e.file_form :scheduel %>

	<%= e.submit('Save') %>
 
<% end %>
